@if(Request.IsAuthenticated) {
    <text>欢迎 <strong>@User.Identity.Name</strong>!
    [ @Html.ActionLink("注销", "LogOff", "Account", new { area=""},null) ]</text>
}
else {
    @:[ @Html.ActionLink("登录", "LogOn", "Account", new { area= "" }, null) ]
}
